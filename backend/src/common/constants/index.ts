// 常量定义统一鉴权与元数据键。
export const PERMISSIONS_KEY = 'permissions';

// 标记接口跳过统一响应包装（用于 SSE 等特殊响应格式）。
export const SKIP_RESPONSE_WRAP_KEY = 'skipResponseWrap';

export const TOKEN_TYPE_ACCESS = 'access';
export const TOKEN_TYPE_REFRESH = 'refresh';
