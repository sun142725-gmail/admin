# 2026-08-06 工作日志

## 退出登录接口排查（22:30）

### 排查结论
- 代码链路完整：`ProfileView.onLogout` → `showConfirmDialog` → `authStore.logout()` → `requestLogout()` → `request.post('/auth/logout')`
- 后端 `/api/auth/logout` 接口存在（无 token 返回 401，属正常）
- Vant Dialog 样式已通过 `van-dialog` 模板使用自动导入
- 问题原因：auth store `logout()` 的 catch 块完全静默，API 失败时错误被吞掉，用户看到直接跳登录页，看起来像没调接口

### 修复
- `stores/auth.js` logout() catch 块增加 dev 模式 `console.warn` 输出错误信息
- 生产构建 `drop_console: true` 不受影响
